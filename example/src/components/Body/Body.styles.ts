import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 'auto',
    paddingTop: 50,
    paddingRight: 10,
    paddingBottom: 20,
    paddingLeft: 10,
    width: '100%',
    maxWidth: 500,
  },
});

export default styles;
