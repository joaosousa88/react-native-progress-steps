import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    paddingTop: 12,
    paddingBottom: 12,
    paddingLeft: 5,
    fontWeight: 'bold',
  },
});

export default styles;
