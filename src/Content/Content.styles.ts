import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 20,
    paddingLeft: 5,
    overflow: 'hidden',
  },
  content: {
    position: 'absolute',
  },
});

export default styles;
