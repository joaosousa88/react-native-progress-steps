import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 20,
    paddingLeft: 5,
    width: '100%',
    overflow: 'hidden',
  },
  content: {
    position: 'absolute',
    width: '100%',
  },
});

export default styles;
